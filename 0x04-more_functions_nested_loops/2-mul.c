#include "main.h"

/**mul - Multiplies the two integers
 * @a: The firsdt number to muliply
 * @b: The second number to multipkty with first number
 * Return: The result of multiplication between a param and b param
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
