#include "main.h"

/**
 * mul - Multiplies the two integers
 * @x: The firsdt number to muliply
 * @y: The second number to multipkty with first number
 * Return: The result of multiplication between x param and y param
 */


int mul(int x, int y)
{
	return (x * y);
}
